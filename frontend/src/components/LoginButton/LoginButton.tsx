function LoginButton() {

    let loginValue = false;

    const handleLogin = async () => {

        loginValue != loginValue;
    }

    return (
        <div>
            <button onClick={handleLogin}>Ir para o login</button>
        </div>
    );
}

export default LoginButton;